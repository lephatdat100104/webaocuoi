@model IEnumerable<WebCUOI.Models.AoCuoi>
@{
    ViewData["Title"] = "Album Áo Cưới";
}

<h1 class="text-center text-primary fw-bold mb-4" style="font-family: 'Playfair Display', serif;">
    💍 Album Áo Cưới
</h1>

<div class="d-flex justify-content-between align-items-center mb-3">
    @if (User.IsInRole("Admin"))
    {
        <a asp-action="Create" class="btn btn-success shadow-sm">
            <i class="fas fa-plus me-2"></i> Thêm Mới Áo Cưới
        </a>
    }

    <div class="input-group w-25">
        <span class="input-group-text bg-white"><i class="fas fa-search"></i></span>
        <input type="search" class="form-control" placeholder="Tìm kiếm..." id="searchInput" />
    </div>
</div>

<div class="row g-4" id="albumContainer">
    @foreach (var item in Model)
    {
        <div class="col-md-4 col-lg-3 col-sm-6 aoCuoiCard">
            <div class="card shadow-sm border-0 h-100">
                <div class="position-relative">
                    <img src="@(!string.IsNullOrEmpty(item.HinhAnhURL) ? item.HinhAnhURL : Url.Content("~/images/no-image.png"))"
                         class="card-img-top"
                         alt="@item.TenAoCuoi"
                         style="height:280px; object-fit:cover; border-top-left-radius:10px; border-top-right-radius:10px;" />
                </div>

                <div class="card-body text-center">
                    <h5 class="card-title fw-bold">@item.TenAoCuoi</h5>
                    <p class="card-text text-danger fw-semibold mb-1">
                        @item.GiaThue.ToString("N0") đ
                    </p>
                    <p class="text-muted small mb-3">
                        Kích cỡ: @item.KichCo
                    </p>

                    <div class="d-flex justify-content-center flex-wrap gap-1">
                        <!-- Nút gộp: Xem / Thuê -->
                        <a asp-action="Details"
                           asp-route-id="@item.ID"
                           class="btn btn-outline-primary btn-sm">
                            <i class="fas fa-eye me-1"></i> Thuê ngay
                        </a>

                        @if (User.IsInRole("Admin"))
                        {
                            <a asp-action="Edit"
                               asp-route-id="@item.ID"
                               class="btn btn-outline-warning btn-sm">
                                <i class="fas fa-edit me-1"></i> Sửa
                            </a>

                            <a asp-action="Delete"
                               asp-route-id="@item.ID"
                               class="btn btn-outline-danger btn-sm"
                               onclick="return confirm('Bạn có chắc muốn xóa áo cưới này không?');">
                                <i class="fas fa-trash-alt me-1"></i> Xóa
                            </a>
                        }
                    </div>
                </div>
            </div>
        </div>
    }
</div>

@section Scripts {
    <script src="https://kit.fontawesome.com/a076d05399.js" crossorigin="anonymous"></script>

    <script>
        // Tìm kiếm theo tên áo cưới
        const searchInput = document.getElementById('searchInput');
        const cards = document.querySelectorAll('.aoCuoiCard');

        searchInput.addEventListener('keyup', function () {
            const filter = this.value.toLowerCase();
            cards.forEach(card => {
                const name = card.querySelector('.card-title').textContent.toLowerCase();
                card.style.display = name.includes(filter) ? '' : 'none';
            });
        });
    </script>
}
